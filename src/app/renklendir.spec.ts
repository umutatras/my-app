import { Renklendir } from './renklendir';

describe('Renklendir', () => {
  it('should create an instance', () => {
    const directive = new Renklendir();
    expect(directive).toBeTruthy();
  });
});
